import cv2
import os

for user in directory:
    user = []
    for img in user:
        cv2.read(img)
        img_name = os.img_name
        name, likes, comments, date, time = img_name[], img_name[], img_name[]
        